FILE (STRINGS "split_func.sql" SQL_SPLIT_FUNC NEWLINE_CONSUME)
STRING (REGEX REPLACE "\n" " " SQL_SPLIT_FUNC ${SQL_SPLIT_FUNC} )

CONFIGURE_FILE (sql.h.in "${CMAKE_CURRENT_BINARY_DIR}/sql.h" @ONLY)

ADD_CUSTOM_TARGET(sql_defs DEPENDS "${CMAKE_CURRENT_BINARY_DIR}/sql.h" )
